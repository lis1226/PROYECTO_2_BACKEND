CREATE TABLE pacientes (
                           id VARCHAR(20) PRIMARY KEY,
                           nombre VARCHAR(100) NOT NULL,
                           fecha_nacimiento DATE,
                           telefono VARCHAR(20)
);